<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina 
            p Coloca tu nuevo password
            
            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="text" name="password" placeholder="Nuevo password" )
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer password")
                //.campo.acciones
                    //a(href="/iniciar-sesion") Ya lo recorde, volver al login 